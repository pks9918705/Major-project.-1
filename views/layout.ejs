<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= title %></title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.css" />

 
  
  <!-- linking the layout.css file  -->
  <link rel="stylesheet" type="text/css" href="/css/layout.css" />
  <!-- css for _header & _footer  -->
  <link rel="stylesheet" type="text/css" href="/css/header.css" />
  <link rel="stylesheet" type="text/css" href="/css/footer.css" />
  <link rel="stylesheet" type="text/css" href="/css/home.css" />
  <link rel="stylesheet" type="text/css" href="/css/sign_in.css" />

  <!-- ********  Important *******  -->
  <!-- you can see only sign-in.css is imported but it works for sign-up because both have the same form name and tag  -->

    <!-- -----------------------------------------------------  -->
 <!-- -------------- adding jquery CDN -------------------  -->
 <!-- -----------------------------------------------------  -->
 <script  src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.0/jquery.min.js"></script>  

  <%- style %>
</head>
<body>
  <%- include('_header'); %>

  <!-- we have to tell where is the variable part  -->
  <main id="layout-main"><%- body %></main>

  <%- include('_footer'); %>


  <script src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.js"></script>

  <!-- script for flash  -->
  <script>
    
    <% if(flash.success && flash.success.length>0 ) { %>
      new Noty({
        theme: 'relax',
        text: "<%= flash.success %>",
        type: 'success',
        layout: 'topCenter',
        timeout: 1500
      }).show();
    <% } %>

    <% if(flash.error && flash.error.length>0 ) { %>
      new Noty({
        theme: 'mint',
        text: "<%= flash.success %>",
        type: 'success',
        layout: 'topCenter',
        timeout: 1500
      }).show();
    <% } %>
  </script>

 

</body>
</html>
